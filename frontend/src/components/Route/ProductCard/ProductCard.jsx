import React from 'react'
import { Link } from 'react-router-dom'

const ProductCard = ({data}) => {
  return (
    <>
    <div>
        <div>
        </div>
        <Link to={`/products/${products._name}`}>
        </Link>
    </div>
    </>
    
  )
}

export default ProductCard